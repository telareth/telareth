# commit-msg:
#   jobs:
#     - name: lint commit msg
#       run: npx commitlint --verbose --edit {1}

# pre-commit:
#   jobs:
#     - name: lint and format
#       run: npx eslint --fix .
#       stage_fixed: true

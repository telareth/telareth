/**
 * Create query for the Health model.
 * @param data The data to save into the Health model.
 */
export async function createHealthEntry(data?: unknown) {
  return data;
}
// export async function postHealth(data?: Client.HealthCreateInput) {
//   const healthData: Client.HealthCreateInput = data ?? {};

//   return client.health.create({ data: healthData });
// }
